{
    "configurations": [
        {
            "name": "Launch Target",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${command:cmake.buildDirectory}",
            "console": "externalTerminal"
        }
    ]
}
